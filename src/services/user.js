import axios from 'axios';
import authHeader from './auth-header'

/*const API_URL = 'http://localhost:3090/api/test/'

const getAdminBoard = () => {
    console.log("Get admin " + API_URL)
    return axios.get(API_URL + 'admin', { headers: authHeader() })
}

const getDoctorBoard = () => {
    console.log("Get doctor " + API_URL)
    return axios.get(API_URL + 'doc', { headers: authHeader() })
}

const getPublicContent = () => {
    console.log("Get public " + API_URL)
    return axios.get(API_URL + 'all')
}

const getUserBoard = () => {
    console.log("Get userBoard " + API_URL)
    return axios.get(API_URL + 'user', { headers: authHeader() })
}

export default {
    getAdminBoard,
    getDoctorBoard,
    getPublicContent,
    getUserBoard,
} */
